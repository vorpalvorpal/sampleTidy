% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spreadsheet-tools.R
\name{vector_from_key}
\alias{vector_from_key}
\title{Extract a vector of cells relative to a labelled key cell}
\usage{
vector_from_key(
  df,
  pattern,
  direction = "right",
  vector_length = NULL,
  ignore_case = TRUE,
  remove_na = TRUE
)
}
\arguments{
\item{df}{a data frame (typically read with \code{col_names = FALSE}).}

\item{pattern}{a regular expression matching exactly one cell.}

\item{direction}{one of \code{"right"}, \code{"left"}, \code{"up"}, \code{"down"}, relative to
the key cell.}

\item{vector_length}{if given, the length the extracted vector (after
\code{remove_na}, if set) must have; a mismatch aborts (class
\code{sampletidy_error}). If \code{NULL} (default), no length check is performed.}

\item{ignore_case}{passed to \code{\link[=str_which_df]{str_which_df()}}.}

\item{remove_na}{drop \code{NA}s from the extracted vector before the
\code{vector_length} check? Default \code{TRUE}.}
}
\value{
a character vector.
}
\description{
Generalised, pure-function port of \code{WEM.data::vector_from_key()}: finds a
single key cell matching \code{pattern} (via \code{\link[=str_which_df]{str_which_df()}}) and returns the
run of cells extending from it in \code{direction}, all the way to the edge of
the data frame.
}
