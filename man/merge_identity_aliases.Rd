% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-alias.R
\name{merge_identity_aliases}
\alias{merge_identity_aliases}
\title{Collapse a redundant identity alias into the feature's \code{self} arm (E.8)}
\usage{
merge_identity_aliases(db = st_config("live_db"), actor, dry_run = FALSE)
}
\arguments{
\item{db}{path to the DuckDB file; defaults to \code{st_config("live_db")}.}

\item{actor}{who is running the merge - mandatory, no default: this DELETEs
registry rows and re-points \code{sample}, and every \code{change_log} row it
writes needs an honest actor.}

\item{dry_run}{if \code{TRUE}, write nothing and return the same itemised
tibble a real run would.}
}
\value{
\code{tibble(alias_key, uuid_winner, uuid_loser, n_repointed)}, one row
per merged duplicate.
}
\description{
For every \code{alias_key} holding both a \code{kind = 'self'} arm and a distinct
non-self arm pointing at the SAME feature, where the key is that feature's
own lower-cased name: carry the \code{confirmed_by} and \code{auto_assign = TRUE}
onto the surviving self arm, re-point every \code{sample.uuid_feature_alias}
reference off the redundant arm, then delete it. The two rows this exists
to remove (\code{b.s01} -> B.S01 and \code{k.e02} -> K.E02) are identity mappings
mislabelled \code{transcription_error} at the 2026-07-23 cutover, before F.19
fixed the labelling; with F.19 in place this is a one-time backfill rather
than a recurring chore.
}
\details{
TRANSACTIONALITY: this is one of the guarded exceptions documented at the
head of this file. duckdb 1.4.1 will not DELETE a \code{feature_alias} row while
a \code{sample} still references it EVEN IF that sample was re-pointed by an
earlier statement of the same transaction (verified), so repoint and delete
must commit separately. Every interruption point is recoverable by simply
re-running: an interruption after the repoint leaves a duplicate arm with
no samples, which the next run merges and deletes; an interruption inside
the FK detach/reattach dance is caught by \code{.fa_torn_guard()} on re-entry.
}
