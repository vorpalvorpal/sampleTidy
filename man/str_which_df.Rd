% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spreadsheet-tools.R
\name{str_which_df}
\alias{str_which_df}
\title{Find matching cells in a data frame}
\usage{
str_which_df(
  df,
  pattern,
  ignore_case = TRUE,
  multiple_matches = FALSE,
  squish = TRUE
)
}
\arguments{
\item{df}{a data frame (typically read with \code{col_names = FALSE}, so every
column is character-ish).}

\item{pattern}{a regular expression to search for in each cell.}

\item{ignore_case}{should the search be case-insensitive? Default \code{TRUE}.}

\item{multiple_matches}{if \code{FALSE} (default) and more than one cell
matches, aborts (class \code{sampletidy_error}). If \code{TRUE}, all matches are
returned.}

\item{squish}{should each cell be run through \code{\link[stringr:str_trim]{stringr::str_squish()}}
before matching (collapses internal whitespace, trims outer)? Default
\code{TRUE}.}
}
\value{
a \code{\link[tibble:tibble]{tibble::tibble()}} with integer columns \code{row} and \code{col}, one row
per matching cell (zero rows if there are no matches).
}
\description{
Generalised, pure-function port of \code{WEM.data::str_which_df()}: searches
every character cell of \code{df} (coercing all columns to character first) for
\code{pattern} and returns the matching cell coordinates. Used to locate
labelled key cells (e.g. \code{"REPORT NO:"}) in raw, header-free spreadsheet
grids such as ACIRL front pages.
}
