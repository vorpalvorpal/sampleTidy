% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dates.R
\name{parse_lab_datetime}
\alias{parse_lab_datetime}
\title{Parse lab-report datetime strings into POSIXct}
\usage{
parse_lab_datetime(x, formats, tz = "Australia/Sydney")
}
\arguments{
\item{x}{character vector of raw datetime strings.}

\item{formats}{either one of the pinned preset names \code{"esdat"},
\code{"crosstab"}, \code{"iso"}, or a character vector of \code{strptime()} format
strings to try directly, in order.}

\item{tz}{timezone to interpret naive lab datetimes in. Defaults to
\code{"Australia/Sydney"} (CONTRACT A9).}
}
\value{
a \code{POSIXct} vector, same length as \code{x}, in \code{tz}.
}
\description{
Tries each format in the resolved \code{formats} preset in order against every
element of \code{x}, keeping the first successful parse; elements that match no
format become \code{NA} (never silently reinterpreted - e.g. \code{"13/13/2025"}
under the \code{"crosstab"} \code{"\%d/\%m/\%Y"} preset is \code{NA}, not rolled over into a
different month/year).
}
