% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/values.R
\name{parse_value}
\alias{parse_value}
\title{Parse raw lab-report value strings into numeric/quantified/RL columns}
\usage{
parse_value(value_raw)
}
\arguments{
\item{value_raw}{character vector of raw value strings as they appear in
the source file.}
}
\value{
a \code{\link[tibble:tibble]{tibble::tibble()}} with one row per element of \code{value_raw} and
columns:
\itemize{
\item \code{value_num} (dbl): the parsed numeric value (equal to \code{rl_low}/
\code{rl_high} for below/above-detection rows).
\item \code{value_chr} (chr): non-numeric free text, \code{NA} otherwise.
\item \code{quantified} (lgl): \code{TRUE} for a genuine numeric or free-text reading,
\code{FALSE} for a below/above-detection or \code{BDL} reading, \code{NA} for a
skipped row.
\item \code{rl_low} (dbl): the reporting limit for \code{<}-notation rows.
\item \code{rl_high} (dbl): the reporting limit for \code{>}-notation rows.
\item \code{skip_reason} (chr): \code{NA} unless the row was skipped
(\code{"empty"}, \code{"no_sample"}, \code{"not_computable"}).
}
}
\description{
Vectorised successor to the old \code{cleanBDLvalues()}. Handles below/above
reporting-limit notation (\verb{<0.01}, \verb{>2000}), the \code{BDL} marker, thousands
separators, and two distinct "not a real reading" cases that must be
recorded as skips with a reason rather than silently dropped (CONTRACT
A4): \code{"NS"} ("no sample") and strings of dashes only (e.g. \code{"----"},
meaning "not computable"). Anything else that isn't numeric is kept
verbatim as free text in \code{value_chr} (e.g. field notes like
\code{"Clear, low flow"}) and marked \code{quantified = TRUE} (a valid, recorded
observation - just not a numeric one).
}
