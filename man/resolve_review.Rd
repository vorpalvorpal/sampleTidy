% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-schema.R
\name{resolve_review}
\alias{resolve_review}
\title{Resolve (close) a single \code{review_queue} row by its own uuid - the operator
API for ANY review kind (Robin's ruling 2, Phase 7b round 3)}
\usage{
resolve_review(uuid, resolution, resolved_by, db = st_config("live_db"))
}
\arguments{
\item{uuid}{the \code{review_queue.uuid} to resolve (the row's own primary key
\itemize{
\item NOT \code{uuid_target}).
}}

\item{resolution}{free-text resolution stored on the closed row.}

\item{resolved_by}{who resolved it - mandatory, no default (A55: every
operator write names who performed it).}

\item{db}{path to the DuckDB file; defaults to \code{st_config("live_db")}.}
}
\value{
\code{invisible(TRUE)} if a row was resolved, \code{invisible(FALSE)} if
\code{uuid} did not match an open row (already resolved, or does not exist).
}
\description{
Before this function existed, NOTHING could close a \code{sample_collision}
review row (Tier-3 finding A4): \code{review_queue_close()} (D4, above) is
unexported, and its one production call site
(\code{.fa_confirm_one_alias()}, \code{R/feature-alias.R}) hard-filters to
\code{kind = "unknown_feature"} - so every OTHER review kind
(\code{sample_collision}, \code{value_conflict}, \code{adapter_tie}, ...) accumulated in
the queue with no operator path to drain it, and the only other
review-facing exports (\code{review_queue()}, \code{review_queue_candidates()}) are
read-only.
}
\details{
\code{resolve_review()} targets a review row by its own PRIMARY KEY
(\code{review_queue.uuid}), not the polymorphic \code{uuid_target}
\code{review_queue_close()} closes on - so there is no \code{kind} ambiguity to
resolve here at all: exactly one row (or none) can ever match a given
\code{uuid}, regardless of what kind raised it. This is deliberately a
DIFFERENT, simpler contract from \code{review_queue_close()} (which stays
internal, for the specific "close everything this confirmation raised"
shape \code{.fa_confirm_one_alias()} needs) rather than a public wrapper around
it.

Writes through the mutation layer (\code{db_update()}, inside a
\code{db_transaction()}), so the close carries a real \code{change_log} provenance
row with \code{actor = resolved_by} - the SAME S6 fix \code{review_queue_close()}
above just received, not the \code{"pipeline"} placeholder.

Idempotent: resolving an already-\code{resolved} (or nonexistent) uuid closes
nothing and does not error - mirrors D6's "closes zero rows, never
errors" contract on \code{review_queue_close()}.
}
