% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{ingest_inbox}
\alias{ingest_inbox}
\title{Ingest an inbox of per-email folders, one batch per folder}
\usage{
ingest_inbox(
  root,
  db = st_config("live_db"),
  dry_run = FALSE,
  reconsider = FALSE
)
}
\arguments{
\item{root}{directory holding one subdirectory per email.}

\item{db}{path to the DuckDB file to ingest into.}

\item{dry_run}{passed through to each \code{\link[=ingest_dir]{ingest_dir()}} call.}

\item{reconsider}{passed through to each \code{\link[=ingest_dir]{ingest_dir()}} call (R-3.7).}
}
\value{
a list with \code{n_folders}, \code{n_folders_failed}, and \code{folders}: a named
list (folder basename -> that folder's \code{ingest_report}, or a
\verb{list(failed = TRUE, error = <message>)} for one that threw).
}
\description{
PowerAutomate saves the attachments of a single lab email into their own
folder under the inbox root, so a folder is the durable record of "these
files arrived together". \code{\link[=ingest_dir]{ingest_dir()}} is deliberately non-recursive (A8,
R-9.5) and stays that way; this walks the root's immediate subdirectories
and calls it once per folder, in sorted order.
}
\details{
Per-folder rather than a \code{recurse = TRUE} flag on \code{ingest_dir()}, for three
reasons each independently sufficient:
\itemize{
\item it keeps one email's files as one batch, which is what makes R-9.8's
"does this folder belong to exactly one work order?" a meaningful
question - flattened into a single batch it would be asking about the
whole inbox, and the real \code{batch-2026-07-23} folder held eight work
orders;
\item a folder that throws is contained, and the rest of the inbox still
commits (the same A27/R-12.1 containment discipline every other per-item
stage in this file follows);
\item it gives R-9.9 a well-defined unit to delete once emptied.
}

Files sitting loose in \code{root} are deliberately NOT ingested: mixing them
into the sweep would make the batch boundary ambiguous, and they are
\code{ingest_dir(root)}'s job if you want them. They are never deleted either,
since nothing here routes them.
}
