% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate.R
\name{add_feature}
\alias{add_feature}
\title{Add a new feature (monitoring point) to the registry}
\usage{
add_feature(
  name,
  site = NA_character_,
  flow = NA_character_,
  matrix = NA_character_,
  geom_wkt = NA_character_,
  virtual = FALSE,
  actor,
  reason
)
}
\arguments{
\item{name}{feature name.}

\item{site}{site name.}

\item{flow}{flow type (e.g. \code{"surface"}), optional.}

\item{matrix}{sample matrix (e.g. \code{"water"}), optional.}

\item{geom_wkt}{optional WKT geometry string.}

\item{virtual}{optional logical flag, defaults to \code{FALSE}.}

\item{actor}{who is making this change.}

\item{reason}{free-text reason, stored on the \code{change_log} row.}
}
\value{
the new feature's uuid, invisibly.
}
\description{
Resolves its own connection via \code{with_db_write(st_config("live_db"))}
(A16) - human-callable, no \code{con} argument. Thin wrapper over \code{db_append()}
with a fresh \code{uuid::UUIDgenerate()} for the new row.
}
