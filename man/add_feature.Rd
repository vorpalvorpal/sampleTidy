% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate.R
\name{add_feature}
\alias{add_feature}
\title{Add a new feature (monitoring point) to the registry}
\usage{
add_feature(
  name,
  site,
  lon,
  lat,
  flow = NA_character_,
  matrix = NA_character_,
  geom_wkt = NA_character_,
  virtual = FALSE,
  actor,
  reason
)
}
\arguments{
\item{name}{feature name.}

\item{site}{site name.}

\item{lon}{longitude, \verb{DOUBLE NOT NULL} on the live schema.}

\item{lat}{latitude, \verb{DOUBLE NOT NULL} on the live schema.}

\item{flow}{flow type (e.g. \code{"surface"}), optional.}

\item{matrix}{sample matrix (e.g. \code{"water"}), optional.}

\item{geom_wkt}{optional WKT geometry string.}

\item{virtual}{optional logical flag, defaults to \code{FALSE}.}

\item{actor}{who is making this change.}

\item{reason}{free-text reason, stored on the \code{change_log} row.}
}
\value{
the new feature's uuid, invisibly.
}
\description{
Resolves its own connection via \code{with_db_write(st_config("live_db"))}
(A16) - human-callable, no \code{con} argument. Thin wrapper over \code{db_append()}
with a fresh \code{uuid::UUIDgenerate()} for the new row.
}
\details{
PLAN-15 F.9/S-15.8: post-001, \code{sample.uuid_feature_alias} is the ONLY path
from a sample to a feature, and migration 001 gave every then-existing
feature a \code{kind = 'self'} alias. A feature created by this function alone
would have none, making it permanently unreachable by its own name
(\code{.rc_feature_candidates()}'s Layer-1 exact match finds zero candidates,
forever and silently - the call still "succeeds") and inert to Work B
Layer 2 (\code{.rc_self_alias()} returns \code{NA}, so a structural hit routes to
REVIEW instead of committing). Fix: write the \code{feature} row AND its
\code{kind = 'self'} \code{feature_alias} row inside ONE mutation-layer transaction
(\code{db_transaction()}, the same participation mechanism \code{commit_event()}
uses), each with its own \code{change_log} provenance row. A failure on the
second write (the alias) rolls the first (the feature) back too - this is
a single atomic unit, not two independent \code{with_db_write()} calls. The
self alias is written already \code{auto_assign = TRUE} and \code{confirmed_by = NA_character_} (Phase-7b round-2 item 11 - NEVER \code{confirmed_by = actor}: a
machine-stamped \code{confirmed_by} is indistinguishable from a human
confirmation, which CONTRACT A55 forbids), with \code{alias_key = .rc_feature_key(name)} matching migration 001's \code{.mig001_normalize()}
convention exactly. Layer-1's exact match reaches this row on
\code{auto_assign} alone (\code{R/reconcile.R:388-398}); it never reads
\code{confirmed_by} at all.
}
