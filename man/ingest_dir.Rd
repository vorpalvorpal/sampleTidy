% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{ingest_dir}
\alias{ingest_dir}
\title{Ingest every file in a flat directory into the sampleTidy database}
\usage{
ingest_dir(path, db = st_config("live_db"), dry_run = FALSE)
}
\arguments{
\item{path}{directory to ingest (non-recursive, A8).}

\item{db}{path to the DuckDB file to ingest into.}

\item{dry_run}{if \code{TRUE}, reconcile but do not commit, snapshot, or
remove anything.}
}
\value{
an \code{ingest_report} (a named list) summarising the run: file
counts by terminal state, events processed/committed, row tallies
(new/already_present/superseded/skipped), review items opened, the
snapshot path (or \code{NA}), and any removed files.
}
\description{
The top-level pipeline (DESIGN Sec1): route -> parse -> assemble ->
reconcile -> commit, run once over every file directly inside \code{path}.
Listing is non-recursive (A8) - subdirectories and their contents are
never touched. Adapters self-register defensively at the start of every
call (A33), so a prior \code{clear_adapters()} can never leave the pipeline
with no adapters.
}
\details{
The whole route/parse/assemble/reconcile/commit sequence runs inside one
\code{\link[=with_db_write]{with_db_write()}} call, using a single read-write connection; that
connection is closed (by \code{with_db_write()}, on return) before
\code{\link[=snapshot_db]{snapshot_db()}} is called - DuckDB is single-writer, so overlapping the
pipeline connection with \code{snapshot_db()}'s own \code{with_db_write()} would
lock-wait and fail.

\code{dry_run = TRUE} still routes, parses, assembles and reconciles (so the
returned report reflects what the run \emph{would} do), but skips
\code{commit_event()} and the snapshot entirely - the only two core-table/
snapshot writers - so a dry run makes zero core-table writes and
produces no snapshot file.

When \code{st_config("remove_ingested")} is \code{TRUE} (default \code{FALSE}, A13) and
this run produced a successful snapshot, every input file whose content
hash has a \emph{verified} archive copy (an \code{asset} row whose archive file
actually exists on disk) is deleted from \code{path}. Cruft/ignored files with
no archive copy, \code{failed}/\code{quarantined} files, and any file whose archive
copy is missing are never deleted. A snapshot failure propagates as an
error (it is not swallowed) and nothing is removed.
}
