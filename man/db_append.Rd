% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate.R
\name{db_append}
\alias{db_append}
\title{Insert rows into a core/ops table, logging one \code{change_log} row per record}
\usage{
db_append(con, table, df, actor, reason, source_hash = NA)
}
\arguments{
\item{con}{an open read-write DBI connection.}

\item{table}{target table name.}

\item{df}{data frame/tibble of rows to insert; must include a \code{uuid}
column for the per-row \code{change_log} linkage.}

\item{actor}{who is making this change.}

\item{reason}{free-text reason, stored on every \code{change_log} row.}

\item{source_hash}{optional content hash provenance (A1), stored on every
\code{change_log} row.}
}
\value{
\code{df}, invisibly.
}
\description{
Validates \code{table} against the mutation-layer allowlist and every column of
\code{df} against the table's real columns, then inserts all rows of \code{df} and
writes one \code{change_log} row per inserted record (\code{action = "insert"},
\code{field = NA}, \code{new} = the row's own \code{uuid}). All log rows from one call
share a single \code{"at"} timestamp. Participates in the caller's open
mutation-layer transaction if there is one (see \code{db_transaction()});
otherwise atomic on its own.
}
