% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pending.R
\name{pending_analytes}
\alias{pending_analytes}
\title{Read dangling \code{lab_method} rows (unresolved incoming analyte/method rows)}
\usage{
pending_analytes(con)
}
\arguments{
\item{con}{an open DBI connection.}
}
\value{
a tibble with stable columns \code{uuid_lab}, \code{name}, \code{organisation},
\code{method}, \code{units_raw}, \code{n_analyses}, even on a zero-row result.
}
\description{
Returns one row per \code{lab_method} with \verb{uuid_analyte IS NULL}, i.e. a
method an event arrived under that has not yet been resolved to a known
\code{analyte}. \code{n_analyses} is a LEFT JOIN + COUNT against \code{analysis} so a
zero-referenced dangling method is still surfaced (never dropped).
\code{units_raw} is this tibble's own output-column name (matching the IR
field it flows into, R/ir.R) - the DB column it reads is
\code{lab_method.units} (D7 reversed, A63); \code{analysis} has no units column.
}
