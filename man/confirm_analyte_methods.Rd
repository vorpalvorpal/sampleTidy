% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-alias.R
\name{confirm_analyte_methods}
\alias{confirm_analyte_methods}
\title{Confirm a lab method's analyte resolution (R-11.11)}
\usage{
confirm_analyte_methods(
  uuid_lab,
  uuid_analyte,
  confirmed_by,
  db = st_config("live_db")
)
}
\arguments{
\item{uuid_lab}{character vector of \code{lab_method.uuid}.}

\item{uuid_analyte}{character vector of \code{analyte.uuid}, same length as
\code{uuid_lab}. \code{NA} is rejected (an analyte must exist).}

\item{confirmed_by}{who is confirming - mandatory, no default (A55).}

\item{db}{path to the DuckDB file; defaults to \code{st_config("live_db")}.}
}
\value{
\code{invisible(tibble(uuid_lab, uuid_analyte, n_analyses, n_converted, action))}.
}
\description{
Sets \code{lab_method.uuid_analyte} for one or more methods, vectorised. No
propagation UPDATE is needed - analyses already point at the method. Each
affected analysis's \code{value}/\code{rl_low}/\code{rl_high} is converted from the
method's recorded \code{units} to the analyte's units via \code{unify_value()}
(D7 reversed - the source units come from \code{lab_method.units}, never from a
per-analysis column). An analysis whose method units cannot be converted
is left alone and opens an \code{unknown_unit} review item. Before converting,
checks \code{change_log} for \code{units}-drift on this method (>1 distinct unit
ever seen); if found, does NOT bulk-convert or link, and opens a
\code{units_drift} review item instead (see PLAN-11 R-11.11 and the RULED PIN
on the drift-detection mechanism).
}
\details{
Re-confirming an already-confirmed method (\code{action = "already_confirmed"})
is a safe no-op ONLY when nothing is left to do. If THIS function set the
link but an earlier call never finished converting every dependent
analysis (interrupted by a crash, or - before this fix - by a sibling
item's failure in the same bulk call), a re-run completes that outstanding
conversion rather than reporting success over it; see the guarded-exception
note at the head of this file. A link set by anything else (e.g. resolved
at ingest) is left untouched, exactly as ever.
}
