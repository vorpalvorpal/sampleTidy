% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/units.R
\name{unify_value}
\alias{unify_value}
\title{Convert values between units}
\usage{
unify_value(value, units_from, units_to)
}
\arguments{
\item{value}{numeric vector of values to convert.}

\item{units_from, units_to}{character vectors of unit strings, each
recycled to \code{length(value)}.}
}
\value{
numeric vector, same length as \code{value}, in the original order.
}
\description{
Pure, vectorised unit conversion. Every element of \code{value} is converted
independently from \code{units_from[i]} to \code{units_to[i]} (both recycled to
\code{length(value)}), using \code{units::set_units(mode = "standard")} internally,
and the result is returned in the original input order.
}
\details{
NA semantics:
\itemize{
\item both \code{units_from[i]} and \code{units_to[i]} \code{NA} -> value unchanged.
\item \code{units_from[i]} \code{NA} (regardless of \code{units_to[i]}) -> value unchanged.
\item \code{units_to[i]} \code{NA} while \code{units_from[i]} is set -> aborts.
}

Identical \code{units_from[i]}/\code{units_to[i]} (after resolving \code{pH}/\verb{pH Unit}/
\code{pH_Units} aliases to the same dimensionless group) short-circuits to the
original value with no pass through udunits, so no floating-point noise is
introduced by an unnecessary round trip.

This function does not resolve analyte/method lookups - callers must
already know the unit strings on both sides. Unlike the ported
\code{WEM.data::unify_value()}, an invalid unit aborts (naming the offending
string) rather than prompting interactively; callers (the reconciler)
catch the error and queue it for review.
}
