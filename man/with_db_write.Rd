% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-connect.R
\name{with_db_write}
\alias{with_db_write}
\title{Run a function against a read-write DuckDB connection, retrying on lock}
\usage{
with_db_write(fn, db = st_config("live_db"), max_wait = 60, every = 2)
}
\arguments{
\item{fn}{function of one argument (the connection); its return value is
returned by \code{with_db_write()}.}

\item{db}{path to the DuckDB file to open read-write.}

\item{max_wait}{maximum total seconds to retry a busy lock before aborting.}

\item{every}{seconds to sleep between retries.}
}
\value{
\code{fn(con)}'s return value.
}
\description{
Opens a read-write connection to \code{db}, retrying every \code{every} seconds (up
to \code{max_wait} seconds total) while the connect error looks like a lock
conflict. A non-lock connect error (e.g. a bad path) aborts immediately.
The connection is always closed on exit, whether \code{fn} succeeds or throws.
Reference implementation: DESIGN.md §9.2.
}
