% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate.R
\name{add_project}
\alias{add_project}
\title{Add a new project (e.g. a work order) to the registry}
\usage{
add_project(
  name,
  type = "Work order",
  uuid_parent = NA_character_,
  uuid_root = NA_character_,
  uuid_project = NA_character_,
  purpose = NA_character_,
  date_start = as.POSIXct(NA),
  date_end = as.POSIXct(NA),
  regulated_by = NA_character_,
  cypher = NA_character_,
  site = NA_character_,
  value = NA_character_,
  actor,
  reason
)
}
\arguments{
\item{name}{project name (work order id for lab-report projects).}

\item{type}{project type, defaults to \code{"Work order"}.}

\item{uuid_parent, uuid_root, uuid_project}{optional project-hierarchy links.}

\item{purpose, site, value, cypher, regulated_by}{optional free-text fields.}

\item{date_start, date_end}{optional \code{POSIXct}/\code{Date} bounds.}

\item{actor}{who is making this change.}

\item{reason}{free-text reason, stored on the \code{change_log} row.}
}
\value{
the new project's uuid, invisibly.
}
\description{
Resolves its own connection via \code{with_db_write(st_config("live_db"))}
(A16). Thin wrapper over \code{db_append()} with a fresh
\code{uuid::UUIDgenerate()} for the new row.
}
