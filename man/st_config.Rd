% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{st_config}
\alias{st_config}
\title{Get or set a sampleTidy configuration value}
\usage{
st_config(key, value)
}
\arguments{
\item{key}{configuration key, e.g. \code{"live_db"}, \code{"input_dir"}.}

\item{value}{if supplied, the value to set for \code{key}.}
}
\value{
the resolved value (get), or \code{value} invisibly (set).
}
\description{
\code{st_config(key)} reads a configuration value; \code{st_config(key, value)} sets
one. Values are stored as R options under the \verb{sampletidy.<key>} prefix.
A get resolves, in order: the option, then the environment variable
\verb{SAMPLETIDY_<KEY>} (key upper-cased), then a built-in default. Keys with
no built-in default abort if neither the option nor the env var is set.
}
