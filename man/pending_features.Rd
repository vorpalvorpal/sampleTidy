% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pending.R
\name{pending_features}
\alias{pending_features}
\title{Read dangling \code{feature_alias} rows (unresolved incoming feature labels)}
\usage{
pending_features(con)
}
\arguments{
\item{con}{an open DBI connection.}
}
\value{
a tibble with stable columns \code{uuid_alias}, \code{name}, \code{alias_key},
\code{n_seen}, \code{n_samples}, \code{first_seen}, \code{last_seen}, even on a zero-row
result.
}
\description{
Returns one row per \code{feature_alias} with \verb{uuid_feature IS NULL}, i.e. an
alias an event arrived under that has not yet been resolved to a known
\code{feature}. \code{n_samples} is a LEFT JOIN + COUNT against \code{sample} so a
zero-referenced dangling alias is still surfaced (never dropped).
}
